apiVersion: apps/v1
kind: Deployment
metadata:
  name:  register-service-app
  labels:
    app:  register-service-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  register-service-app
  template:
    metadata:
      labels:
        app:  register-service-app
    spec:
      containers:
      - name:  register-service-app
        image: hiransanjeewa/springboot-backend-config-server:register-service
        ports:
        - containerPort: 9002

---

apiVersion: v1
kind: Service
metadata:
  name: register-service-svc
spec:
  selector:
    app.kubernetes.io/name: register-service-app
  ports:
    - port: 80
      targetPort: 9002